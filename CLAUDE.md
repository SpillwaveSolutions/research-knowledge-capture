# CLAUDE.md — research-knowledge-capture

Same plugin as AGENTS.md. Claude Code loads `.claude-plugin/plugin.json`.

Do not invent RKC types. Do not write `draws_from`. Run `scripts/rkc_validate.py` after edits under `knowledge/research/**`.
